# Copy this file to .env, then replace the empty value with your own API key.
# .env is ignored by Git and is read into the shell before starting the server.
MONEY_GRAPH_LLM_URL=https://api.openai.com/v1/chat/completions
MONEY_GRAPH_LLM_MODEL=gpt-4.1-mini
MONEY_GRAPH_LLM_API_KEY=
